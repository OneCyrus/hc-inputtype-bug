schema {
  query: Query
  mutation: Mutation
}

type Mutation {
  addPerson(p: PersonInput!): Person!
}

type Person {
  name: String!
}

type Query {
  person: Person!
}

input PersonInput {
  name: String!
}